try:
    import readline
except ImportError:
    print("Module readline not available.")
else:
    import rlcompleter
    readline.parse_and_bind("tab: complete")

try:
    from see import see
except ImportError:
    print("Module 'see' not available.")

